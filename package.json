{
  "name": "ablements-token-parser",
  "version": "1.0.0",
  "description": "Figma design token parser",
  "scripts": {
    "generate": "pnpm install && node scripts/cli.mjs"
  },
  "author": "Ablements",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^5.0.0",
    "fs-extra": "^11.2.0",
    "simple-git": "^3.27.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
